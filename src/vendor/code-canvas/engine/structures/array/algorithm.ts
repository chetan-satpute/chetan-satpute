import { algorithmFor, operationFor } from '../../algorithm.ts';
import { CoreArray } from './structure.ts';

export const defineArrayAlgorithm = algorithmFor(CoreArray);
export const defineArrayOperation = operationFor(CoreArray);
